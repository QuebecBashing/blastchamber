name: Injection - workflow_runs

on:
  workflow_run:
    workflows:
      - trigger

jobs:
  a:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event.workflow_run.head_commit.author.name }}
      
      
