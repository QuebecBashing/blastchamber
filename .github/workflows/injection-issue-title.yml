name: Injection - Issue Title
on:
  issues:

env:
  FLAG: ${{ secrets.FLAG }}

permissions: {}

jobs:
  a:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ env.TITLE }}
        env:
          TITLE: ${{ github.event.issue.title }}
